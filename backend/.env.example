PORT=4000
CORS_ORIGINS=http://localhost:5173,http://localhost:4173
NODE_ENV=development

# Destino de los mensajes del formulario
CONTACT_TO=dannym2407@gmail.com

# Gmail SMTP (recomendado — FormSubmit falló con tokens inválidos)
# 1) En Google: activa verificación en 2 pasos
# 2) https://myaccount.google.com/apppasswords → genera una clave de 16 dígitos
# 3) Pégala en SMTP_PASS (sin espacios)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=dannym2407@gmail.com
SMTP_PASS=
